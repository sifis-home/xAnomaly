#  ____  _   _  ___ _     ____
# | __ )| | | ||_ _| |   |  _ \
# |  _ \| | | | | || |   | | | |
# | |_) | |_| | | || |___| |_| |
# |____/ \___/ |___|_____|____/

name: xAnomaly
run-name: Official build started by ${{ github.actor }}
on:
  push:
    branches:
      - '*'
jobs:
  build:
    runs-on: build
    env:
      LOCAL_BUILD: false
      DOCKER_REGISTRY: harbor.sensative.net/yggio-tools/
      DOCKER_IMAGE: harbor.sensative.net/yggio-tools/xanomaly:latest
      REGISTRY_USER: 'robot$yggio-tools+builder'
      REGISTRY_PASSWORD: ${{ secrets.HARBOR_PASSWORD }}
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js 18.12.1
        uses: actions/setup-node@v3
        with:
          node-version: 18.12.1
      - run: docker login -u $REGISTRY_USER -p $REGISTRY_PASSWORD $DOCKER_REGISTRY
      - run: docker build -t $DOCKER_IMAGE .
        working-directory: ./xAnomaly/serving
      - run: docker push $DOCKER_IMAGE
        working-directory: ./xAnomaly/serving
